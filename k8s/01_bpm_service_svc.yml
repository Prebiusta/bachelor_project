apiVersion: v1
kind: Service
metadata:
  name: bpm-service-svc
spec:
  selector:
    app: bpm-service
  ports:
    - port: 8000
      targetPort: 8000
      name: http
      protocol: TCP
  type: LoadBalancer
